# Blitztext Cross-Platform — Python dependencies
# Install with: pip install -r requirements.txt

# GUI
PyQt6>=6.6.0

# Audio recording
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.24.0

# HTTP (async, for API calls)
httpx>=0.27.0

# Secure credential storage
keyring>=24.0.0

# Global hotkeys (optional but recommended)
# On Linux, requires a display server (X11 or Wayland with uinput)
pynput>=1.7.6

# Optional: local Whisper transcription (no API key needed)
# faster-whisper>=1.0.0

# Optional: auto-paste on Windows
# pyautogui>=0.9.54
